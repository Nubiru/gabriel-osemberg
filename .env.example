# Database — SQLite (auto-creates if not exists)
DATABASE_URL=sqlite:gabriel_osemberg.db?mode=rwc

# Server (overridden by Leptos config in Cargo.toml for dev, fly.toml for production)
# LEPTOS_SITE_ADDR=127.0.0.1:3000
# LEPTOS_SITE_ROOT=target/site

# Logging
# RUST_LOG=info
